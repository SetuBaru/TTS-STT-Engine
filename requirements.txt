# SilkyVoice – API server (transcription + TTS). Install on Linux: pip install -r requirements.txt

# --- Web API ---
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# --- Speech-to-text (Whisper worker) ---
faster-whisper>=1.0.0

# --- Text-to-speech (Coqui XTTS-v2, Arabic & English) ---
TTS>=0.22.0
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.33.0

# --- Shared / runtime ---
numpy>=1.24.0
scipy>=1.10.0

# --- Text-to-speech (NAMAA-Saudi-TTS) ---
chatterbox
huggingface_hub>=0.20.0
safetensors>=0.4.0
